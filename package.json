{
    "name": "bharat-verify",
    "private": true,
    "workspaces": [
        "apps/*",
        "packages/*"
    ],
    "scripts": {
        "dev": "npm run dev --workspace=apps/web",
        "build": "npm run build --workspace=apps/web",
        "lint": "npm run lint --workspace=apps/web",
        "contract:compile": "npm run compile --workspace=packages/contracts",
        "contract:deploy": "npm run deploy --workspace=packages/contracts",
        "contract:test": "npm run test --workspace=packages/contracts"
    },
    "devDependencies": {
        "typescript": "^5.3.3"
    },
    "engines": {
        "node": ">=18.0.0"
    }
}